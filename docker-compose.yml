version: "3.9"

services:
  frontend:
    image: tekiro-frontend-app
    container_name: tekiro-nextjs
    build: .
    restart: always
    ports:
      - "3001:3000"
    env_file:
      - .env.production